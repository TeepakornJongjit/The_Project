# M01 — User & Student Management

Owner: Team member 1

Put M01 feature code here. Keep database access through the shared `lib/` Supabase client and follow the requirements in `docs/14_team_requirements.md`.

Do not commit `.env.local` or service-role/secret keys.
